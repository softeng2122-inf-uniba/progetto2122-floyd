package it.uniba.app.utils;

/**
 * {@literal <<noECB>>}
 * A class that implements the Resettable interface can
 * reset its state to default.
 */
public interface Resettable {
    
    /** Sets the object to default state. */
    void reset();
}
